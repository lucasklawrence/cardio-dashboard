# CLAUDE.md

## Commands
- `npm run lint` — ESLint check
- `npm run format:check` — Prettier check
- `npm run build` — TypeScript + Vite build
- `npm test` — Vitest test suite

## Conventions
- Avoid compound shell commands (chained with `&&`, `;`, `||`). Prefer separate, individual commands.
